# R0.1 Proof-Gated Mission Core — review handoff

## Scope

This branch bootstraps a reconstructed, independently testable STARCOM core because the historical source bytes were not mounted in the execution runtime.

Implemented in this slice:

- deterministic canonical receipts;
- append-only SHA-256-linked SQLite ledger;
- default-deny Trust Plane with deterministic precedence and one-use grants;
- role-separated Proof Engine;
- proof-gated Mission Kernel;
- durable at-least-once effects with idempotency, leases and bounded retries;
- Task 5 pre-request attempt ledger and monotone wave enforcement;
- persistent JSON CLI vertical slice;
- repository verification, secret scan, manifest and pinned CI action.

## Truth retained

```text
PRODUCT_NOT_IMPLEMENTED
NO_EXTERNAL_RUNTIME_INTEGRATED
NO_COMPONENT_ADOPTION
LIVE_800_PLUS_CENSUS_NOT_CERTIFIED
TASK5_DISPOSITION = RECOLLECT_REQUIRED
```

The branch does not claim to contain the unavailable historical implementation or to close C1 through C7.

## Verification expected

```bash
PYTHONPATH=src:. python3 scripts/verify_repo.py
PYTHONPATH=src:. python3 -m unittest discover -s tests -v
python3 -m compileall -q src tests scripts
```

Local fresh evidence before publication reported 78 passing tests, a verified SHA-256 manifest, and zero detected committed secrets. GitHub Actions must independently reproduce the repository gate before this PR can be promoted.

## Review gates

- [ ] GitHub Actions green on the exact head commit.
- [ ] No bootstrap transport files remain.
- [ ] No secrets, private keys, local databases or generated state are committed.
- [ ] Canonical blocked states remain fail-closed.
- [ ] Author/verifier/certifier separation is enforced.
- [ ] Research row-to-ledger reconciliation detects valid-data tampering.
- [ ] At-least-once behavior is not mislabeled exactly-once.
