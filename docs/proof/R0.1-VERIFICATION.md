# STARCOM R0.1 Verification Record

**Verification date (UTC):** 2026-08-13
**Source commit under test:** `2670aa02e236ff3a42447f8b0f3fc659c2c9cbff`
**Branch:** `bootstrap/r0.1-proof-gated-core`
**Release boundary:** R0.1 — Proof-Gated Mission Core

## Truth state

This record proves only the reconstructed R0.1 vertical slice in this repository. It does not certify the unavailable historical source tree or the complete STARCOM product.

```text
PRODUCT_NOT_IMPLEMENTED
NO_EXTERNAL_RUNTIME_INTEGRATED
NO_COMPONENT_ADOPTION
LIVE_800_PLUS_CENSUS_NOT_CERTIFIED
TASK5_DISPOSITION = RECOLLECT_REQUIRED
C1_INDEPENDENT_REVIEW = REPORTED_COMPLETE
C1_RECOVERY_PUBLICATION = NOT_PROVEN_EXECUTED_IN_THIS_RUNTIME
```

## Environment

- Linux `6.18.35`, `x86_64`
- CPython `3.13.5`
- Python executable: `/opt/pyvenv/bin/python3`
- SQLite `3.46.1`
- Runtime dependencies: none
- Build dependency: `setuptools==82.0.1`
- Verification state directory: fresh temporary directory, removed after the run

## Fresh commands and results

| Command | Result |
|---|---:|
| `STARCOM_DB=<temporary>/starcom.sqlite3 PYTHONPATH=src:. python3 scripts/verify_repo.py` | PASS, exit 0 |
| `PYTHONPATH=src:. python3 -m unittest discover -s tests -v` | PASS, 78 tests, exit 0 |
| `python3 scripts/secret_scan.py --root .` | PASS, 0 findings, exit 0 |
| `python3 scripts/build_manifest.py --root . --json` | PASS, 35 entries checked, exit 0 |
| `python3 -m compileall -q src scripts tests` | PASS, exit 0 through verifier |

The complete captured output is in `docs/proof/R0.1-TEST-OUTPUT.txt`.

## Requirement-to-evidence map

| Requirement or risk | Observable evidence | Result |
|---|---|---:|
| Deterministic canonical JSON and SHA-256 | Canonical vectors, UTC normalization, unsupported-type rejection | PASS |
| Tamper-evident immutable ledger | Hash-chain tests, optimistic-head conflict, forced SQLite mutation detection | PASS |
| Default-deny authorization | Default deny, wildcard allow, deny override, expiry, mission scope, single-use grants | PASS |
| Authorization verification fail-closed | Malformed request/rule JSON is reported as defects; decision row is reconciled with its ledger event | PASS |
| Role-separated proof | Author/verifier/certifier separation, missing evidence rejection, certificate tamper detection | PASS |
| Proof verifier fail-closed | Malformed evidence metadata returns a deterministic integrity defect rather than an exception | PASS |
| Proof-gated mission state | Legal/illegal transitions, decision binding/non-reuse, idempotency, terminal certificate gate | PASS |
| Honest durable delivery | At-least-once lease model, stale-worker rejection, retry exhaustion, expired-lease recovery | PASS |
| Task 5 structural repair | Attempt persisted before request control returns, monotonic waves, missing receipt/linkage defects | PASS |
| Research row↔ledger integrity | Valid-JSON field mutations, timestamps, hashes, streams, event kinds and malformed JSON are detected | PASS |
| End-to-end vertical slice | Research → trust authorization → mission execution → durable effect → proof → terminal success | PASS |
| Machine-readable operations | CLI JSON success/error contracts and cross-process SQLite persistence | PASS |
| Supply-chain/repository policy | Exact dependency pins, full-SHA action pin, secret scan, no database artifacts/placeholders | PASS |
| Deterministic source inventory | `MANIFEST.sha256` verified with no missing, mismatched, or unlisted source files | PASS |

## Evidence interpretation

The checks establish that the R0.1 implementation satisfies its documented vertical-slice contracts on the recorded local environment. The verifier is fail-closed: compilation, tests, repository policy, secret scanning, and manifest verification must all pass for exit code 0.

The proof certificate in R0.1 is a deterministic digest-backed application record. It is not yet a public-key digital signature or an accepted external trust-root certificate.

## Residual limitations

- The complete historical STARCOM source/archive was not mounted; this is a reconstructive bootstrap, not a byte-for-byte import.
- C1 independent review is continuity-reported, but recovery publication is not proven executed in this runtime.
- C2 live census/recollection has not run and the 800+ census is not certified.
- SQLite is the real R0.1 adapter but is intentionally single-process; Neon/PostgreSQL remains a future tested adapter.
- The durable engine implements at-least-once delivery with idempotency, not exactly-once external effects.
- No Temporal server, external model runtime, OpenCode/Hermes/OpenClaw component, Web UI, desktop, or mobile application is integrated.
- Tests do not exercise live network sources, hostile multi-process contention, cross-platform Windows/macOS behavior, or production-scale load.
- SonarQube MCP/CLI and a container runtime were unavailable in this execution environment; no SonarQube result is claimed.
- Release Candidate and full product gates remain open.

## Hash scope

`docs/proof/R0.1-FILE-HASHES.sha256` hashes the source-manifest scope, `MANIFEST.sha256`, this report, and the captured test output. The hash file excludes itself because a file cannot contain its own stable cryptographic digest.

## Post-generation replay

Replay initiated at `2026-08-13T08:56:17Z` after all proof documents were regenerated:

- `PYTHONPATH=src:. python3 scripts/verify_repo.py` exited `0`.
- `sha256sum -c docs/proof/R0.1-FILE-HASHES.sha256` exited `0` for all listed files.
